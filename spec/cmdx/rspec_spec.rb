# frozen_string_literal: true

RSpec.describe Cmdx::Rspec do
  it "has a version number" do
    expect(Cmdx::Rspec::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
